name: appmovilutils
description: "aplicaion que usa varias cosas del telefono"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.3


dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  url_launcher: ^6.3.1
  geolocator: ^13.0.1
  qr_flutter: ^4.1.0
  qr_scanner: ^0.0.2-259.9.beta
  mobile_scanner: ^6.0.1
  sensors_plus: ^6.1.0
  torch_light: ^1.1.0
  speech_to_text: ^7.0.0
  flutter_tts: ^4.0.2

  proximity_sensor: ^1.3.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0


flutter:
  uses-material-design: true

  assets:
    - assets/images/villoimg.jpg